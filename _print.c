#include "main.h"
/**
 * _printf -
 * @format:
 * Return: Result
*/
int _printf(const char *format, ...)
{
	int i;
	va_list args;
	va_start(args, format);

	format == '\0'
	return(0);

	format = va_list(args, i);

	for (i = 0; i < format; i++)
	

